<%= form_with(model: [@hoby, @post, Comment.new], local: true) do |form| %>
  <div class="comment-area">
    <%= form.text_area :content, class: 'comment-text-area' %>
  </div>
  <div class="actions">
    <%= form.submit t('comment.add_comment') %>
  </div>
<% end %>
